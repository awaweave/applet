.open --new weave.db

create table user(
 user_id text primary key,
 name text,
 password text
);

create table talk_log(
 id integer primary key autoincrement,
 user_id text,
 talk text,
 foreign key(user_id) references user(user_id)
);

insert into user values("user1", "うさきち", "password");
insert into user values("user2", "うさたろう", "password");
insert into talk_log(user_id, talk) values("user1", "ねぇねぇ、帰ってくるのおそいんだけど！！今どこいまどこいまどこーー");
insert into talk_log(user_id, talk) values("user1", "今から帰るよー");
insert into talk_log(user_id, talk) values("user2", "了解！寝過ごさないようにだよ〜風呂ためとこ");
